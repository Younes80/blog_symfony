{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        <div class="row mt-5">
            <div class="col-md-12">
                <h1>{{post.title}}</h1>
                <img src=" {{ post.image }} " alt="">
            </div>
        </div>
    </div>

{% endblock %}